import { Link, Route } from "react-router-dom";
import ContentsListPageComponent from "./ContentsListPageComponent.js";

// Returns a list of routes (which contiain the generated list page components)
export default function ContentsListPage(title, pageContents, routeNames) {
  let routes;
  return routes;
}
